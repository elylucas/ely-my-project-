
/**
 * Says Hello
 * @param name name
 */
export const sayHello = (name: string): string => {
  return 'Hello ' + name;
};


export const sayHello2 = (name: string, age: number): string => {
  return 'Hello ' + name + ' 2aaaaaaa You are ' + age;
};